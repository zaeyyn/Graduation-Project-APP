name: flutter_application_1
description: "LinkGuard - Phishing Link Protection"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  http: ^1.2.1
  fl_chart: ^0.68.0
  url_launcher: ^6.3.0
  telephony: ^0.2.0   # NEW: automatic SMS sending

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true